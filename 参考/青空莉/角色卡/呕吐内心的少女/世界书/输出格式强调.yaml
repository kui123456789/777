---
变量更新&状态栏格式强调:
  rule: The following must be inserted to the end of reply, and cannot be omitted
  format: |-
    <UpdateVariable>
    ...
    </UpdateVariable>
    <StatusBlock>
    <small>
    ```yaml
    ...
    ```
    </small>
    </StatusBlock>
