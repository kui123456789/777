interface:
  display_name: "777"
  short_description: "Repo-specific patterns and workflows for 777"
  default_prompt: "Use the 777 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true